{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for Memo Capture bootstrap.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
